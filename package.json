{
  "name": "creatorlens-proxy",
  "version": "1.0.0",
  "description": "Proxy server bridging CreatorLens artifact to Apify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
